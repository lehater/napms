# Plans

Only current execution state belongs here.

`active/README.md` is the compact current execution capsule. Detailed plans are created only when the selected current increment needs them and are removed when their durable outcome has been absorbed into requirements/domain/architecture/engineering truth.

Git history is the plan archive. Do not retain completed roadmaps or previous execution packets in the working tree.
