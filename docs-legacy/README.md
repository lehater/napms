# Retired legacy documentation

Status: RETIRED / non-canonical.

This directory is retained only as frozen migration evidence from the pre-cutover documentation system. It is not a source of current product, domain, architecture, implementation-readiness, process, or execution truth.

The sole canonical documentation namespace is `docs-v2/`.

Rules:

- do not add or update design truth here;
- do not use files below this directory to override accepted `docs-v2/` anchors;
- use Git history when historical provenance is required;
- new design work, corrections, validation state and Harness coordination belong under `docs-v2/`.

The legacy files remain physically present only to preserve migration provenance. Their former authority ended with H20 namespace cutover on 2026-09-17.
