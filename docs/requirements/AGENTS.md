# Requirements documentation scope

Apply root `AGENTS.md` and `docs/AGENTS.md` first.

Requirements define accepted observable product behavior and quality. Domain identity/lifecycle belongs under `docs/domain/`; implementation/runtime mechanics belong downstream in architecture/engineering/code.
