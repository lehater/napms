# Application Communication Catalogue domain scope

Apply root `AGENTS.md` and `docs/AGENTS.md` first.

This directory owns Tactical DDD for ACC identity, structure, lifecycle and invariants.

Do not place HTTP, PostgreSQL or Web concerns here. Preserve stable Component Deployment and DCS identities consumed by downstream contexts. Strategic ownership changes must re-enter `docs/domain/strategic-model.md` / `semantic-ownership.md` before changing this tactical model.
