version: 1
kind: validation-evidence
id: H20-NAMESPACE-CUTOVER
status: PASS
recorded_at: '2026-09-17'
scope: documentation-authority-namespace
stakeholder_authorization:
  granted: true
  evidence: explicit user instruction to continue after H20 was presented as requiring authorization for physical documentation namespace cutover
result:
  canonical_namespace: docs-v2/
  retired_namespace: docs/
  retired_namespace_mode: frozen-migration-evidence-only
  reverse_sync: forbidden
  competing_current_truth_store: false
  product_code_changed: false
  product_tests_changed: false
  merge_performed: false
checks:
  - docs-v2/README.md declares docs-v2 as sole canonical documentation authority.
  - docs/README.md declares the old tree retired and non-canonical.
  - top-level AGENTS.md routes agents to docs-v2 workstream state, roadmap, specs and accepted anchors rather than legacy docs.
  - H16-H19 accepted design truth remains under docs-v2 without semantic rewrite during cutover.
  - legacy files remain available only for migration provenance and cannot override accepted docs-v2 truth.
  - product implementation and product-test authorization remain absent.
notes:
  - The cutover retires rather than bulk-deletes legacy evidence. This preserves provenance while removing its authority and avoids a destructive file-by-file migration that would add no design value.
  - The canonical namespace retains the docs-v2 name; H20's required property is one obvious authority, not cosmetic renaming.
