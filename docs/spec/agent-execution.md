# Agent execution specification — superseded

Status: SUPERSEDED by H9 Task Capsule v2.

The active machine-readable execution/context contract is:

- `docs-v2/spec/agent-execution-context-v2.yaml`
- contract id: `DOCS-V2-AGENT-EXECUTION-CONTEXT-V2`

The previous M4 prose specification is preserved in Git history and was used as a redesign input for H9. It is no longer an independent source of current Harness execution semantics.

Do not implement or validate agent execution behavior from this file. Resolve the active H9 contract and its version-bound dependencies instead.
