version: 1
kind: meta-workstream-state
id: DOCS-V2-HARNESS-STATE
roadmap: docs-v2/meta/roadmap.yaml
branch: meta/docs-v2-m7-pilot
last_updated: 2026-09-16

program:
  status: ACTIVE
  current_phase: H4
  current_phase_state: DIRTY
  completed_phases: [H0, H1, H2, H3, H5]
  blocked_phases: []
  compatibility_review_required_phases: [H5]

current_task:
  id: H4-T2
  title: Complete S2 artifact types required by H6
  state: NOT_STARTED
  objective: >
    Close the artifact-registry gap exposed by H6 preflight. Add canonical S2 artifact
    ownership for Bounded Context identity/boundary semantics and Domain Process Discovery /
    Event Storming semantics before H6 defines their detailed schemas and derivation methods.
    Preserve H3/H5 identity, lineage and dependency invariants, avoid duplicate canonical truth,
    and perform a focused H5 compatibility review after the H4 delta. Do not execute H6 in the
    same bounded capsule.
  expected_outputs:
    - retained bounded-context S2 anchor type with one canonical owner/representation
    - retained domain-process-model S2 anchor type with one canonical owner/representation
    - applicability and G2 relevance for both types
    - explicit disposition of capability-to-context mapping as canonical refs/projection rather than duplicate truth
    - explicit disposition of candidate aggregate discovery as process-model hypothesis/input to tactical domain-model unless a separate canonical type is proven necessary
    - H5 compatibility review for registry/dependency/lineage contracts
  next_after_completion: Resume H6-T1 after H5 compatibility review passes

completed_work:
  - {id: H0-T1, result: PASS, artifact: docs-v2/meta/roadmap.yaml, summary: durable roadmap and redesign charter}
  - {id: H0-T2, result: PASS, artifact: docs-v2/meta/workstream-state.yaml, summary: resumable repository-resident workstream state}
  - {id: H1-T1, result: PASS, artifact: docs-v2/spec/harness-conformance.yaml, summary: initial RED-stage Harness conformance/eval contract}
  - {id: H1-T2, result: PASS, artifact: docs-v2/spec/harness-conformance.yaml, summary: lifecycle transition and rejection coverage hardening}
  - {id: H1-T3, result: PASS, artifact: docs-v2/spec/harness-reliability-conformance.yaml, summary: deterministic replay, restart safety, idempotency, fail-closed and regression hardening}
  - {id: H2-T1, result: PASS, artifact: docs-v2/spec/lifecycle-state-machine.yaml, summary: executable lifecycle and gate state machine}
  - {id: H3-T1, result: PASS, artifact: docs-v2/spec/design-anchor.yaml, summary: universal Design Anchor contract}
  - {id: H4-T1, result: PASS-HISTORICAL-NOW-DIRTY, artifact: docs-v2/spec/artifact-type-registry.yaml, summary: artifact type registry before H6 completeness gap was exposed}
  - {id: H5-T1, result: PASS, artifact: docs-v2/spec/dependency-registry-impact.yaml, summary: generated registry, typed dependencies, fingerprints and impact model; compatibility review required after H4-T2}
  - {id: H5-T2, result: PASS, artifact: docs-v2/spec/design-derivation-lineage.yaml, summary: universal logical derivation lineage and project-book projection contract; compatibility review required after H4-T2}

routing_history:
  - id: H2-T1-ATTEMPT-1
    result: REOPEN(H1)
    observed_at: 2026-09-16
    reason: Initial H1 lacked complete lifecycle transition/rejection coverage required by H2.
    resolution: H1-T2 added missing RED-stage coverage; H2 then completed.
  - id: H6-PREFLIGHT-RELIABILITY-AUDIT
    result: REOPEN(H1)
    observed_at: 2026-09-16
    reason: Existing H1 lacked explicit reliability conformance for the universal Harness quality bar.
    resolution: H1-T3 closed the gap without requiring H2-H5 reopen.
  - id: H6-T1-PREFLIGHT-1
    result: REOPEN(H4)
    observed_at: 2026-09-16
    reason: >
      H6 roadmap requires bounded-context specifications and a durable Domain Process Discovery /
      Event Storming model. The H4 registry has context-relationship-map and tactical domain-model,
      but no retained bounded-context anchor and no retained domain-process-model anchor. Because
      context-map is intentionally a generated projection, there is currently no canonical owner
      for Bounded Context identity/boundary truth; process-discovery semantics likewise have no
      canonical artifact family. H6 cannot invent these types without violating H4 ownership.
    evidence:
      - docs-v2/meta/roadmap.yaml#H6
      - docs-v2/spec/artifact-type-registry.yaml
      - docs-v2/spec/artifacts.md
    resolution: >
      Route one bounded H4-T2 task to complete the registry, then perform focused H5 compatibility
      review. Resume H6 only after that passes.

carry_forward:
  - Harness is a universal project-agnostic design execution rail from idea/problem/evidence through requirements, DDD, architecture and implementation readiness before first product code; NAPMS is a pilot, never a hard-coded semantic special case.
  - Harness is a critical engineering instrument: explicit versioned rule or explicit stable stop; silent heuristic fallback is forbidden.
  - docs-v2/spec/harness-reliability-conformance.yaml is mandatory downstream design input for H6-H13 where routing, validation, execution, regression, CI or runtime behavior is specified.
  - Project documentation is a graph of machine-readable Design Anchors and typed relations; human-readable documentation is a reproducible non-canonical book/view projection.
  - Provenance alone is insufficient: every accepted non-root anchor preserves logical derivation lineage with accepted predecessors/evidence/constraints/decisions, derivation kind and durable design rationale.
  - Accepted roots require durable stakeholder/external/evidence/constraint authority; agent plausibility cannot be an unexplained root.
  - H5 impact dependencies and derivation lineage reuse the same H3 semantic refs and anchor identities; no parallel hand-maintained lineage graph is allowed.
  - H4-T2 must introduce no project-specific domain semantics; it only completes universal artifact families required by the DDD process.
  - bounded-context must own Bounded Context identity/boundary/responsibility semantics; context-map remains a generated projection and context-relationship-map owns inter-context relationship semantics.
  - domain-process-model must own durable process-discovery/Event Storming semantics such as actors, commands, domain events, policies, timelines/hotspots and external systems; detailed H6 schema/method follows only after H4 type ownership exists.
  - capability-to-context mapping should be derivable from explicit H5 refs between accepted capability-map and bounded-context anchors and exposed as a projection/index, not duplicated as a second canonical mapping owner.
  - candidate aggregate discovery should remain explicit durable process knowledge but must not become an accepted tactical aggregate merely because an agent proposed a boundary; H4-T2 must decide the smallest non-duplicating carrier and H6 later defines derivation rules.
  - Domain Events discovered in S2 are domain semantics and are not automatically S3 integration messages/event-contracts.
  - docs/ remains canonical product/project design truth until explicit cutover; ACCEPTED docs-v2 anchors do not override docs/ before cutover.
  - Product code/tests remain excluded as sources of product/domain/architecture truth during current documentation-design work.
  - H2 lifecycle-state-machine owns redesigned lifecycle semantics; no new lifecycle transition class/rejection path may appear outside H1 coverage.
  - H3 design-anchor owns identity/provenance/acceptance/human-control/freshness invariants.
  - H4 artifact-type-registry owns artifact classification, fixed semantic owners, applicability and canonical-format selection.
  - H5 dependency-registry-impact owns generated registry, typed refs, fingerprint/canonicalization, validation-basis and impact semantics.
  - S2 G2 is eligible only when all applicable strategic/process-discovery/tactical checkpoints are ACCEPTED; non-applicable checkpoints are skipped, not fabricated complete.
  - Deterministic structural/reference/authority/self-consistency failures must be resolved before semantic evaluation can PASS.

blockers: []

restrictions:
  product_implementation_authorization: none
  executable_test_change_authorization: none
  workflow_ci_change_authorization: none
  canonical_cutover_authorization: false
  merge_authorization: false

resume_protocol:
  - Read this file first.
  - Read only the current phase entry and direct dependencies from docs-v2/meta/roadmap.yaml.
  - Load existing specs only when required by the current task.
  - Treat carry_forward required-input pointers as explicit context dependencies for the current phase.
  - Complete one bounded META task, validate it, persist state, and stop.
  - Do not infer progress from chat history when repository state is available.
