version: 15
kind: meta-workstream-state
id: DOCS-V2-HARNESS-STATE
roadmap: docs-v2/meta/roadmap.yaml
branch: meta/docs-v2-m7-pilot
last_updated: 2026-09-17
program:
  status: ACTIVE
  current_phase: H16
  current_phase_state: MIGRATING_STRATEGIC_DDD
  completed_phases: [H0, H1, H2, H3, H4, H5, H6, H7, H8, H9, H10, H11, H12, H13, H14, H15]
control_plane:
  contract: {id: DOCS-V2-HARNESS-CONTROL-PLANE, path: docs-v2/spec/harness-control-plane.yaml, version: 1, revision: 3}
  rule: meaningful design work is visible; runtime reliability bookkeeping is internal
current_work:
  phase: H16
  name: Whole-domain strategic DDD migration and revalidation
  state: IN_PROGRESS
  predecessor_ref: docs-v2/meta/h15-integration-plan.yaml
  next: extract the accepted whole-domain strategic DDD baseline from frozen docs evidence into canonical docs-v2 S2 anchors, reconciling the already-revalidated Resource Catalogue and Authority Management anchors rather than duplicating them
  source_evidence:
    - docs/domain/strategic-model.md
    - docs/domain/strategic-model.json
    - docs/domain/context-map.md
    - docs/domain/capabilities.md
    - docs/domain/semantic-ownership.md
  target_scope: NAPMS strategic domain landscape across all target Bounded Contexts
documentation_authority:
  evolving_authority: docs-v2/
  legacy_source: docs/
  legacy_status: frozen-read-only-migration-input
  reverse_sync: forbidden
  namespace_cutover: planned-after-strategic-migration-and-mvp-design
carry_forward:
  - Human-owned truth is never invented; material gaps become questions/blockers.
  - Design documentation is a graph; affected-only revalidation replaces waterfall restart.
  - Downstream design conforms to applicable upstream authoritative constraints.
  - Readiness gates are lifecycle guarantees not separate execution units.
  - H14 minimal runtime and H15 shared docs-v2 validation entrypoint are complete and passing.
  - Canonical design anchors may be YAML documents or Markdown documents with YAML front matter.
  - Pre-H12 Task Capsule validators remain legacy-only and outside the docs-v2 validation path.
  - H12 Resource Catalogue strategic truth already accepted in docs-v2 must be reconciled into the whole-domain strategic baseline, not replaced by stale legacy wording.
  - Do not continue isolated Resource Catalogue S3 work until the whole-domain strategic baseline and narrow MVP journey are established.
  - After strategic migration, tactical/requirements work narrows to the confirmed MVP journey: resources plus application/components/interactions -> policy rule -> materialized/exported policy.
restrictions:
  product_implementation_authorization: none
  product_test_change_authorization: none
  product_code_tests_as_design_truth: forbidden
  namespace_cutover_authorization: false
  merge_authorization: false
blockers: []
resume_protocol:
  - Read this file first then roadmap.yaml.
  - During H16 read only the frozen strategic DDD evidence directly needed for the current strategic anchor plus already-accepted docs-v2 anchors that overlap it.
  - Treat docs/ only as frozen migration evidence; never edit it and never let it override accepted docs-v2 corrections.
  - Materialize broad strategic S2 truth across the domain before resuming narrow MVP tactical/architecture work.
  - Preserve the accepted minimal runtime boundary and use changed/affected validation.
  - Product code/tests remain outside current authority and cannot supply missing design truth.
  - Ask the stakeholder only when migration exposes a material contradiction or missing human-owned strategic truth.
